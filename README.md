# Neural Mech

A minimal Python server for browsing assets and their future digital twins.

Run it with:

```sh
python3 server.py
```

Then open <http://127.0.0.1:8000>. The Assets page is available at `/`, `/home`,
and `/assets`; the HVAC asset links to `/digital-twin/HVAC-001`.
